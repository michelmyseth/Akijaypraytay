import { NextApiRequest, NextApiResponse, Redirect } from "next";
export default async (request: NextApiRequest, response: NextApiResponse) => {
  // const updateToken = await mongodb
  // .db()
  // .collection("users")
  // .updateOne(
  //   { "profile.mail": `${userInfoResponse.email}` },
  //   {
  //     $set: {
  //       "profile.token": "",
  //     },
  //   }
  // );
};
